{
  "name": "codingChallengeForWorkspace",
  "version": "0.0.1",
  "private": true,
  "engines": {
    "node": "8.6.0"
  },
  "scripts": {
    "start": "node ./bin/www",
    "dev": "concurrently \"nodemon ./bin/www\" \"cd ./client  && npm start \" ",
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "body-parser": "^1.18.3",
    "concurrently": "^3.6.1",
    "cookie-parser": "~1.4.3",
    "debug": "~2.6.9",
    "dotenv": "^4.0.0",
    "express": "^4.16.3",
    "hbs": "~4.0.1",
    "mongoose": "^5.3.1",
    "morgan": "^1.9.1",
    "serve-favicon": "~2.4.5"
  }
}